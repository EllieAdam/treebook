<hr>
<p class="action">
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong>
    <i class="fa fa-pencil"></i>
    <% if activity.owner %>
      <%= link_to activity.owner.name, profiles_path(activity.owner) %>
    <% else %>
      Deleted user
    <% end %>
  </strong>
  <% if a.trackable %>
    edited a <%= link_to "status", status_path(a.trackable) %>.
  <% else %>
    edited a status that is now deleted.
  <% end %>
</p>