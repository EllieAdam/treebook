<p class="action">
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong>
    <i class="fa fa-minus"></i>
    <% if activity.owner %>
      <%= link_to activity.owner.name, profiles_path(activity.owner) %>
    <% else %>
      Deleted user
    <% end %>
  </strong>
  <% if a.trackable %>
    cancelled an upvote for a <%= link_to "status", status_path(a.trackable) %>.
  <% else %>
    cancelled an upvote for a status that no longer exists.
  <% end %>
</p>
